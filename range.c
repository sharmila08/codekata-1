#include <stdio.h>

int main(void) {
int n;
printf("enter the number between 1 to 9");
scanf("%d",&n);
if(n>=1&&n<=9)
{
printf("the number is between the range");
}
else
printf("the number is not between the range");

	return 0;
}

